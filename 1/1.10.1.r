# Установите пакет phonTools и введите команду pickIPA(n = 1). К какому ряду
# относится в выведенном на экран инвентаре гласный a?

install.packages('phonTools')
library(phonTools)
pickIPA(n=1)
detach(phonTools)
