# Используя функцию sample() (подробнее смотрите ?sample()) напишите команду,
# симулирующую бросок двух кубиков. Помните, что значения на двух кубиках могут
# повторяться, например, при set.seed(42) значения на обоих кубиках равны шести.
# (не больше 5 строчек кода)

sample(1:6, size = 2, replace = TRUE)
